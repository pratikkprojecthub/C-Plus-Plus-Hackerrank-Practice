#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int a;
    vector<int> v;
    cin >> a;
 
     for (int i = 0; i < a; i++) {
        int t;
        cin >> t;
        v.push_back(t);
    }
    
    sort(v.begin(), v.end());
    
    for (auto x : v)
        cout << x << " ";  
    return 0;
}
